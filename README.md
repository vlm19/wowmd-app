# wowMD App

Private repository for the wowMD website, feedback API, localization files, and deployment configuration.

The current website code lives in `website/`.

The Web App source lives in `app/`. Version `0.3.0` adds the extension handoff import flow for public GitHub Markdown raw URLs.
