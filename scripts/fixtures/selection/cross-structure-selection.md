# Structured selection regression

## Mixed inline content

START_INLINE select across **bold content**, [linked content](https://example.com), and `inline code`, then END_INLINE here.

## Embedded non-text content

START_IMAGE before the image ![non-text separator](https://example.com/non-text-separator.png) continue after it, then END_IMAGE here.
